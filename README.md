# alos-platform

Laravel application with Vuexy Bootstrap 5 admin template.

## Setup

```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
```

## Run

```bash
php artisan serve
```

Then open `http://localhost:8000`
